(toplevel_expect_tests
 ((libraries (ppx_hash_lib))))

(library
 ((name ppx_hash_test)
  (libraries (core_kernel))))

;  (preprocess (((pps (ppx_hash ppx_inline_test ppx_assert ppx_sexp_conv)) All)))))

(alias ((name DEFAULT)
        (deps (inline_records.ml.pp
               override_example.ml.pp hash_test.ml.pp record_field_control.ml.pp))))
